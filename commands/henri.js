const discord = require("discord.js")

module.exports.run = async (bot, message, args) => {

    return message.channel.send("oh dus je wilt ontslag? ik kan er voor zorgen dat je dubbel ontslag krijgt :) ");
}

module.exports.help = {
    name: "henri"

}