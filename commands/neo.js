const discord = require("discord.js")

module.exports.run = async (bot, message, args) => {

    return message.channel.send("als je nu niet stopt ruk ik je kop eraf");

}

module.exports.help = {
    name: "neo"

}