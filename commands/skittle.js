const discord = require("discord.js")

module.exports.run = async (bot, message, args) => {

    return message.channel.send("I will fix all your problems *10 minutes later*  spreek je bericht in na de piep. : P");

}

module.exports.help = {
    name: "mathias"

}