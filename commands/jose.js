const discord = require("discord.js")

module.exports.run = async (bot, message, args) => {

    return message.channel.send("ruth heeft mijn kroketten gejat");

}

module.exports.help = {
    name: "jose"

}